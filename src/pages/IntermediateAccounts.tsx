import DataSourceEmptyState from "@/components/DataSourceEmptyState";

const IntermediateAccounts = () => (
  <DataSourceEmptyState
    title="Intermediate Accounts"
    description="Account records will appear here after an accounts data source is connected."
  />
);

export default IntermediateAccounts;
