import DataSourceEmptyState from "@/components/DataSourceEmptyState";

const BsAdpSubjects = () => (
  <DataSourceEmptyState
    title="BS / ADP Subjects"
    description="Subjects will appear here after a subjects data source is connected."
  />
);

export default BsAdpSubjects;
